export class User {
  // tslint:disable-next-line:variable-name
  constructor(public avatar_url: string, public login: string, public bio: string, public followers: string, public following: string) {}
}
